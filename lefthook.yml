pre-commit:
  commands:
    eslint:
      glob: "*.{js,ts,jsx,tsx}"
      run: bun eslint {staged_files}
    prettier:
      glob: "*.{js,ts,jsx,tsx,vue}"
      run: bun prettier {staged_files}
commit-msg:
   scripts:
    'commit_check.sh':
      runner: bash 
